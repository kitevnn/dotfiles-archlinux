;; 
;; plug-ivy: Ivy 增强原生emacs操作功能
;; 

;; (use-package Ivy :demand)

;; (require 'ivy)
;; 
;; (ivy-mode)
;; (setq ivy-use-virtual-buffers t)
;; (setq enable-recursive-minibuffers t)
;; enable this if you want `swiper' to use it
;; (setq search-default-mode #'char-fold-to-regexp)

;; (provide 'plug-ivy)
